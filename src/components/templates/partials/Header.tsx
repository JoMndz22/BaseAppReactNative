import { View } from 'react-native'
import { P } from '../../atoms'

const Header = () => {
    return (
        <View style={{ backgroundColor:'green' }}>
            <P color='red' fsize={20}>Header ANDA</P>
        </View>
    )
}

export default Header